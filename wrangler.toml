name = "sucaiku"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# SUPABASE_URL 和 SUPABASE_SERVICE_KEY 请在 Cloudflare Dashboard 的
# Workers > Settings > Environment Variables 中配置，不要硬编码在此文件中。
